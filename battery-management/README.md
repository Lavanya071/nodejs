# Battery Management API

This is an API for managing battery data. It provides endpoints to store and retrieve battery information, including `current`, `voltage`, and `temperature`.

## Setup

1. Clone this repository.
2. Install dependencies:

   ```bash
   npm install
